// Public API surface for schema-audit.
// Real exports land in cycle 1 — Section 3 (Core validation logic).
// This stub exists so tsup can build a valid package skeleton during Section 1.

export const VERSION = "0.1.0";
